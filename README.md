# Sentiment-analysis
A Python-based project that analyzes the sentiment of YouTube video titles using NLTK’s VADER sentiment analyzer. It automatically fetches videos from YouTube channels via the YouTube Data API, computes sentiment scores (positive, negative, neutral, compound), and visualizes the results through interactive plots.
